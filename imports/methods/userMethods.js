import { Meteor } from "meteor/meteor";
/*
import Users from "../publishers/userPublisher";

Meteor.methods({
   "questions.insert" (){
       
       Questions.insert({
           text,
           createdAt: new Date(),
           owner: Meteor.userId(),
           username: Meteor.user().username
       });
   },
    
    "questions.remove" (questionId){
        
        Questions.remove(questionId);
    }
    
});
*/
